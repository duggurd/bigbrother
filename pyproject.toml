[project]
name = "bigbrother"
version = "0.1.0"
description = "Activity Tracking & Session Monitoring"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psutil>=6.0.0",
    "pywin32>=306",
]

[dependency-groups]
dev = [
    "pillow>=10.0.0",
    "pyinstaller>=6.0.0",
]
